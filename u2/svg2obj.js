class SvgObjConverter{
    /*! Converter function
     * @param svgFile : a DOM-loaded file object
     * @param properties : an object containing the properties of the conversion:
     *   + mode: 
     *     - "maze" : the only supported now. All the lines in the SVG file will be converted
     *                to 3D vertexed walls in the OBJ. 
     *                Related properties: wallHeight, wallWidth
     *       
     */
    convertToObj(svgFile, properties){


    }
}
